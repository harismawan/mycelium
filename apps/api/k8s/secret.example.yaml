apiVersion: v1
kind: Secret
metadata:
  name: mycelium-secrets
type: Opaque
stringData:
  # CHANGE THESE VALUES BEFORE APPLYING
  DATABASE_URL: "postgresql://user:password@postgresql:5432/mycelium?schema=public"
  # Redis — dedicated instance outside Kubernetes. Percent-encode username/password if they contain URL-reserved characters.
  REDIS_URL: "redis://:CHANGEME@192.168.100.22:6379"
  JWT_SECRET: "change-this-to-a-secure-random-string"
  MYCELIUM_API_KEY: "change-this-to-a-secure-api-key"
